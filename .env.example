# PostgreSQL connection string for the optional Drizzle database workflow.
# The application currently uses in-memory storage at runtime.
DATABASE_URL=

# HTTP server configuration.
PORT=5000
NODE_ENV=development
